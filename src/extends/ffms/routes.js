/**
 * All available routes for the app
 */

export const baseUrl = '/ffms';

export const FFMS = `${baseUrl}`;

export const MAPLAYERS = `${baseUrl}/map-layers`;

export const JOBS = `${baseUrl}/jobs`;

export const LOCATION = `${baseUrl}/location`;

export const ROUTE_PLANNING = `${baseUrl}/direction`;

export const DASHBOARD = `${baseUrl}/dashboard`;

export const JOBS_MANAGER = `${baseUrl}/jobs-manager`;

export const EMPLOYEES_MANAGER = `${baseUrl}/employees-manager`;

export const HISTORY = `${baseUrl}/history`;

export const SEARCH = `${baseUrl}/search`;

export const CUSTOMER_MANAGER = `${baseUrl}/customer-manager`;

export const REPORT = `${baseUrl}/report`;

export const WORKER = `${baseUrl}/worker`;


export const REGISTER = `${baseUrl}/invite`;

export const USER_LOGIN = `${baseUrl}/welcome`;

export const WELCOME_PAGE = `${baseUrl}/start`;

export const INVITE_PAGE = `${baseUrl}/invite`;

export const DOWNLOAD_PAGE = `${baseUrl}/download`;

export const ROLES_MANAGER = `${baseUrl}/roles-manager`;

export const APP_CONFIG = `${baseUrl}/app-config`;

export const MANAGER_LAYER_DATA = `${APP_CONFIG}/data-manager`;

export const TENANT_INFO = `${APP_CONFIG}/app-info`;

