/**
 * All available routes for the app
 */
const dashboardRoute = 'ffms/dashboard';

export const HOME = `${dashboardRoute}`;

export const DASHBOARD_TYPE = `${dashboardRoute}/:dashboard-type`;

export const DASHBOARD_MODE = `${dashboardRoute}/:dashboard-type/:dashboard-mode`;
